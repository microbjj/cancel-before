services:
  postgres:
    image: postgres:16
    container_name: cancel-before-postgres
    restart: always
    environment:
      POSTGRES_USER: cbd
      POSTGRES_PASSWORD: cbd
      POSTGRES_DB: cbd
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: